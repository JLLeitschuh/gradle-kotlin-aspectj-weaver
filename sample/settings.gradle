pluginManagement {
    resolutionStrategy {
        eachPlugin {
            if (requested.id.namespace == 'org.jlleitschuh.gradle.kotlin-aspectj') {
                useModule("org.jlleitschuh.aspectj:kotlin-aspectj-weaver:${requested.version}")
            }
        }
    }
}

includeBuild("../plugin")
